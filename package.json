{
  "name": "snowflake-school-wellbeing-demo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "generate": "node apps/generator/src/cli.ts",
    "test": "node --test apps/generator/test/*.test.ts && pnpm --dir apps/dashboard test",
    "typecheck": "node --run generate -- --help && pnpm --dir apps/dashboard typecheck"
  }
}
